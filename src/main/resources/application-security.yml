spring:
  config:
    activate:
      on-profile: security

# Access control is defined in SecurityConfig.java (Spring Security's
# authorizeHttpRequests). The previous `security.whitelist` YAML list was
# replaced because it could not distinguish between safe public GETs and
# dangerous unauthenticated writes on the same path prefix.
