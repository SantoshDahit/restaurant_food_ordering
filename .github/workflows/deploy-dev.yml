name: deploy-dev

on:
  push:
    branches:
      - dev
  workflow_dispatch:

concurrency:
  group: deploy-dev
  cancel-in-progress: true

env:
  PROFILE: dev
  PROJECT_NAME: restaurant-food-ordering-system
  APPLICATION_NAME: api
  DOCKER_HUB_USERNAME: santosh390
  CONTAINER_PORT: 8080
  DOMAIN: 217.15.165.238
  MYSQL_CONTAINER_NAME: shared-mysql
  DOCKER_NETWORK: restaurant-network
  CADDY_NETWORK: web

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.set_outputs.outputs.image_tag }}
    steps:
      - name: Checkout dev branch
        uses: actions/checkout@v3
        with:
          ref: ${{ env.PROFILE }}

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build application jar
        run: ./gradlew clean build -x test

      - name: Set image tags
        run: |
          DATE_TAG=$(date +'%Y.%m.%d')
          DATED_TAG="${{ env.DOCKER_HUB_USERNAME }}/${{ env.PROJECT_NAME }}-${{ env.APPLICATION_NAME }}:${{ env.PROFILE }}.$DATE_TAG"
          echo "DATED_TAG=$DATED_TAG" >> $GITHUB_ENV
          echo "DATE_TAG=$DATE_TAG" >> $GITHUB_ENV
          echo "🔨 Building Docker image with tag: $DATED_TAG"
          docker build -t $DATED_TAG .

      - name: Set outputs for next job
        id: set_outputs
        run: echo "image_tag=${{ env.DATED_TAG }}" >> $GITHUB_OUTPUT

      - name: Docker Hub Login
        uses: docker/login-action@v2
        with:
          username: ${{ env.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Docker Hub Publish
        run: |
          echo "📤 Pushing Docker image..."
          docker push ${{ env.DATED_TAG }}

          echo "🧹 Cleaning up old dated images on Docker Hub..."
          TOKEN=$(curl -s -H "Content-Type: application/json" -X POST \
            -d "{\"username\": \"${{ env.DOCKER_HUB_USERNAME }}\", \"password\": \"${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}\"}" \
            https://hub.docker.com/v2/users/login/ | jq -r .token)

          if [ -z "$TOKEN" ]; then
            echo "❌ Failed to get Docker Hub token"
            exit 1
          fi

          PAGE=1
          ALL_TAGS=""
          while true; do
            RESPONSE=$(curl -s -H "Authorization: JWT $TOKEN" \
              "https://hub.docker.com/v2/repositories/${{ env.DOCKER_HUB_USERNAME }}/${{ env.PROJECT_NAME }}-${{ env.APPLICATION_NAME }}/tags/?page=$PAGE&page_size=100")
            PAGE_TAGS=$(echo "$RESPONSE" | jq -r ".results[] | select(.name | startswith(\"${{ env.PROFILE }}.\")) | .name")
            if [ -z "$PAGE_TAGS" ]; then break; fi
            ALL_TAGS="$ALL_TAGS $PAGE_TAGS"
            HAS_NEXT=$(echo "$RESPONSE" | jq -r '.next')
            if [ "$HAS_NEXT" == "null" ]; then break; fi
            PAGE=$((PAGE+1))
          done

          CURRENT_DATE_TAG="${{ env.PROFILE }}.${{ env.DATE_TAG }}"
          for TAG in $ALL_TAGS; do
            if [ "$TAG" != "$CURRENT_DATE_TAG" ]; then
              echo "🗑️ Removing old tag: $TAG"
              curl -s -X DELETE -H "Authorization: JWT $TOKEN" \
                "https://hub.docker.com/v2/repositories/${{ env.DOCKER_HUB_USERNAME }}/${{ env.PROJECT_NAME }}-${{ env.APPLICATION_NAME }}/tags/$TAG/"
              sleep 1
            fi
          done

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DEV_PC_HOST }}
          username: ${{ secrets.DEV_PC_USERNAME }}
          key: ${{ secrets.DEV_PC_SSH_KEY }}
          port: ${{ secrets.DEV_PC_SSH_PORT }}
          script: |
            set -e
            echo "🚀 Deploying ${{ env.PROFILE }} on VPS..."

            # ─── Docker Hub login ───
            echo "${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}" | docker login -u "${{ env.DOCKER_HUB_USERNAME }}" --password-stdin

            # ─── Pull new image ───
            IMAGE_TAG="${{ needs.build.outputs.image_tag }}"
            if [[ -z "$IMAGE_TAG" ]]; then
              echo "❌ IMAGE_TAG is empty!"
              exit 1
            fi
            echo "🔄 Pulling: $IMAGE_TAG"
            docker pull $IMAGE_TAG

            # ─── Ensure required networks exist ───
            # MySQL must already be running on ${{ env.DOCKER_NETWORK }} (managed outside this workflow).
            # Caddy must already be running on ${{ env.CADDY_NETWORK }}.
            docker network inspect ${{ env.DOCKER_NETWORK }} >/dev/null 2>&1 || docker network create ${{ env.DOCKER_NETWORK }}
            docker network inspect ${{ env.CADDY_NETWORK }} >/dev/null 2>&1 || docker network create ${{ env.CADDY_NETWORK }}

            # ─── Stop and remove old app container ───
            APP_CONTAINER=${{ env.PROJECT_NAME }}-${{ env.APPLICATION_NAME }}-${{ env.PROFILE }}
            docker stop $APP_CONTAINER 2>/dev/null || true
            docker rm $APP_CONTAINER 2>/dev/null || true

            # Clean up dangling images
            docker images -f "dangling=true" -q | xargs -r docker rmi -f || true

            # ─── Start app container ───
            # Connects to MySQL via the shared docker network — MySQL is reachable at
            # ${{ env.MYSQL_CONTAINER_NAME }}:3306 on ${{ env.DOCKER_NETWORK }}.
            echo "🚀 Starting app container..."
            docker run -d \
              --name $APP_CONTAINER \
              --network ${{ env.DOCKER_NETWORK }} \
              --restart unless-stopped \
              -e SPRING_PROFILES_ACTIVE=${{ env.PROFILE }} \
              -e SPRING_DATASOURCE_URL="jdbc:mysql://${{ env.MYSQL_CONTAINER_NAME }}:3306/${{ secrets.MYSQL_DATABASE }}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul" \
              -e SPRING_DATASOURCE_USERNAME=${{ secrets.MYSQL_USER }} \
              -e SPRING_DATASOURCE_PASSWORD=${{ secrets.MYSQL_PASSWORD }} \
              -e AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }} \
              -e AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }} \
              -e JWT_SECRET=${{ secrets.JWT_SECRET }} \
              $IMAGE_TAG

            # ─── Attach app to Caddy reverse-proxy network ───
            docker network connect ${{ env.CADDY_NETWORK }} $APP_CONTAINER 2>/dev/null || true

            echo "📊 Running containers:"
            docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Status}}" | grep -E "${{ env.PROJECT_NAME }}|${{ env.MYSQL_CONTAINER_NAME }}|caddy" || true

            echo "🎉 Deployment successful!"
